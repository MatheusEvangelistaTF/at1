#include <stdio.h>

int main(){
    float valorFloat;
    int valorInteiro;

    printf("Digite um valor em ponto flutuante (float): ");
    scanf("%f", &valorFloat);

    valorInteiro = (int)valorFloat;

    printf("Valor convertido para inteiro: %d\n", valorInteiro);

    return 0;
}
